/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.dummyjson.com', 'www.pexels.com'],  // הוסף את שמות ה-hostnames ישירות
    },
};

export default nextConfig;
